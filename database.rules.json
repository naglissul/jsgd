{
    "rules": {
        ".read": "auth != null",
        ".write": false,
        "players": {
            "$uid": {
                ".write": "auth != null && auth.uid == $uid"
            }
        },
        "enemys": {
            ".write": "auth != null"
        },
        "bullets": {
            ".write": "auth != null"
        }
    }
}
